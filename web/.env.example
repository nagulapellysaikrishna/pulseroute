# Optional: PulseRoute Worker URL (defaults to built-in simulation / mock if empty)
NEXT_PUBLIC_PULSEROUTE_API_URL=https://pulseroute.your-subdomain.workers.dev

# Optional: PostgreSQL Database URL (Neon/Supabase) for direct metrics querying
DATABASE_URL=postgresql://user:password@ep-sample.us-east-2.aws.neon.tech/neondb?sslmode=require
